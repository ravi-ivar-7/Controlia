import React from 'react';

const Footer = () => {

  const footerStyle = {
    backgroundColor: '#222831',
    color: '#fff',
    textAlign: 'center',
    padding: '1rem',
    marginTop: 'auto',
  };

  return (
    <footer style={footerStyle}>
      <div>
        © Controlia
      </div>
    </footer>
  );
};

export default Footer;
